# Daylight Commander — UI/UX 설계 (v0.1)

[PLAN.md](PLAN.md)(기능/MVP), [ARCHITECTURE.md](ARCHITECTURE.md)(구조)를 기반으로 한 UI/UX 초안. 비주얼 톤은 같은 제작자의 [saturn-mobile-client-flutter](../saturn-mobile-client-flutter) 테마를 계승하되, 다크 퍼스트가 아닌 **라이트/다크 동등 지원**으로 조정한다. 목업 이미지는 대화창에서 확인 (다크/라이트 2-pane 레이아웃, 파일 유형별 색상 아이콘 칩).

## 1. 디자인 원칙
- **아이콘이 정보다**: 파일 유형을 텍스트가 아니라 색상+아이콘으로 먼저 인지하게 한다 (Saturn의 기기별 색상 코딩 패턴을 파일 유형에 적용).
- **라이트/다크 동등**: 두 테마 모두 같은 `_base()` 팩토리에서 파생 — 한쪽만 신경 쓴 티가 나지 않게 대비/채도를 각각 맞춘다.
- **유리질감(Glass) 계승, 과하지 않게**: Saturn의 반투명 카드 톤은 패널 배경에 은은하게만 적용하고, 파일 리스트 자체는 밀도 높은 실무 도구답게 플랫하게 유지한다.
- **키보드 우선, 마우스 병행**: Total Commander 사용자가 위화감 없게 단축키 체계를 표준으로 삼는다.

## 2. 컬러 시스템 (Saturn 팔레트 계승)

| 역할 | Dark | Light |
|---|---|---|
| Background | `#0A0E14` | `#F4F6FA` |
| Surface (패널/카드) | `#151D27` | `#FFFFFF` |
| Surface 강조 | `#1D2733` | `#E3E8EF` |
| Border | `#FFFFFF` 10% | `#000000` 8% |
| Border 강조 | `#FFFFFF` 20% | `#000000` 14% |
| Primary (브랜드/폴더) | `#4C9DFF` | `#2C6BE0` |
| Accent (네트워크 드라이브 등) | `#7C5CFF` | `#7C5CFF` |
| Success | `#34D399` | `#34D399` |
| Warning | `#FFB020` | `#FFB020` |
| Danger | `#FF5A5F` | `#FF5A5F` |
| Text 고강조 | `#F1F5F9` | `#101828` |
| Text 중강조 | `#A9B4C4` | `#5B6676` |
| Text 저강조 | `#6B7787` | `#5B6676` (알파 낮춰 사용) |

Saturn과 동일하게 `AppColors` + `AppRadius` + `AppTheme.dark()/.light()` 구조를 그대로 재사용한다 (`lib/core/theme.dart` 패턴 이식).

## 3. 파일 유형별 아이콘 컬러 코딩

> **구현 후 수정**: 프로젝트에 실제로 준비되어 있던 `icons8 "Windows 11 Color"` 컬러 SVG 세트(`assets/icons/filetypes/`, 37종 + 폴더 3종)를 그대로 채택했다. 이 아이콘들은 이미 그 자체로 여러 색을 쓴 일러스트라서(단색 글리프가 아님) 아래 "카테고리 색상"은 실제 아이콘 렌더링에 쓰이지 않고, **매칭되는 컬러 아이콘이 없는 확장자를 위한 폴백 로직/향후 배지·상태 표시용 기준색**으로 의미가 바뀌었다. 정확한 매핑은 `lib/presentation/theme/file_type_style.dart` 참고.

| 카테고리 | 실제 아이콘 | 폴백 시 기준색 (Dark / Light) |
|---|---|---|
| 폴더 | `assets/icons/folders/icons8-folder.svg` (Documents/Games 폴더는 테마 폴더 아이콘) | Primary `#4C9DFF` / `#2C6BE0` |
| 네트워크 드라이브(SMB) | (P1에서 서버 아이콘 추가 예정) | Accent `#7C5CFF` |
| 이미지 (jpg/png 등) | `icons8-jpg-240.svg`, `icons8-png-240.svg` | `#FFC24B` / `#B4750B` |
| 비디오 (avi/mov/mpg 등) | `icons8-avi-240.svg` 등 | `#38BDF8` / `#2590C5` |
| 오디오 (mp3/wav 등) | `icons8-mp3-240.svg` 등 | `#22D3EE` / `#1BA8BC` |
| 압축파일 (zip/7z/rar/tar) | `icons8-zip-240.svg` 등 | `#C084FC` / `#7C5CFF` |
| 코드/개발 파일 (json/html/css/java/sql 등) | `icons8-json-240.svg` 등 | `#4ADE80` / `#3B9959` |
| 문서/PDF (pdf/doc/xls/ppt/txt) | `icons8-pdf-240.svg` 등 | `#FF5A5F` / `#D9373C` |
| 실행파일/앱 (exe/apk/dll) | `icons8-exe-240.svg` 등 | `#FF7A59` / `#C2531F` |
| 매칭 없는 파일(알 수 없음) | `icons8-hexadecimal-240.svg` (범용 폴백) | 중립 `#6B7787` / `#5B6676` |
| 즐겨찾기 | (Material `star`) | Warning `#FFB020` |

**아이콘 렌더링 방식**: 컬러 SVG를 22×22로 배경/틴트 없이 그대로 표시 (Windows 탐색기처럼 아이콘 자체가 이미 식별력을 가짐). 당초 설계였던 "틴트 배경 칩"은 단색 아이콘 세트를 전제로 한 것이라 폐기했다.

## 4. 아이콘 시스템 (패키지 선택)

> **구현 후 수정**: 당초 `phosphor_flutter` 채택을 계획했으나, 2.1.0(pub.dev 최신)이 `PhosphorIconData extends IconData`로 구현되어 있고 현재 Flutter SDK(3.47)에서 `IconData`가 `final class`로 바뀌면서 컴파일이 깨지는 것을 빌드 중 확인했다. 대안 패키지도 같은 제약을 받을 가능성이 높아, **툴바/메뉴 등 UI 크롬 아이콘은 Material Icons로 되돌렸다** (Saturn도 실제로는 Material Icons만 사용 — 조사 결과 확인됨). 파일 유형 아이콘은 위 3장의 컬러 SVG 세트(`flutter_svg`)를 그대로 사용하므로 이 이슈와 무관하다.
- 사이즈 스케일: 리스트 행 22 (파일 아이콘) / 툴바 액션 18 / 사이드바 드라이브 트리 16 / 빈 상태 일러스트 48
- 툴바/메뉴 액션 아이콘 색상은 `textMid`(비활성) / `Primary`(활성, 호버) 사용
- 향후 phosphor_flutter가 SDK 호환 버전을 내면 재검토 가능 (교체 지점은 `pane_view.dart`/`home_screen.dart`의 `Icons.*` 사용처로 한정되어 있어 마이그레이션 비용 낮음)

## 5. 타이포그래피

- **UI 크롬(버튼, 메뉴, 상태바, 다이얼로그)**: Saturn과 동일한 커스텀 폰트 **SeoulNamsan** (Light/Regular/Bold/ExtraBold) 재사용 — 브랜드 일관성.
- **파일/폴더 이름**: 시스템 기본 폰트(플랫폼별 fallback, 예: SF Pro / Segoe UI / Noto Sans)로 별도 지정. 파일명은 사용자가 통제할 수 없는 임의의 유니코드(한글 외 CJK, 이모지, 특수기호)를 포함하므로 SeoulNamsan의 커버리지 밖 문자가 깨지는 것을 방지하기 위함.
- 크기: 리스트 행 13 / 컬럼 헤더 11.5(medium) / 주소창·경로 12 / 상태바 11 / 다이얼로그 제목 16(bold)

## 6. 레이아웃 구조

```
┌─ 타이틀바 (커스텀, window_manager) ───────────────────────────┐
├─ 툴바: [뒤로][앞으로][상위] [주소창(좌)] | [주소창(우)] [검색] ┤
├───────────────────┬─────────────────────────────────────────┤
│  왼쪽 패널          │  오른쪽 패널                              │
│  ┌ 컬럼헤더 ─────┐  │  ┌ 컬럼헤더 ─────┐                        │
│  │ 이름/크기/수정 │  │  │ 이름/크기/수정 │                        │
│  ├──────────────┤  │  ├──────────────┤                        │
│  │ [칩]폴더/파일  │  │  │ [칩]폴더/파일  │  ← 아이콘 칩 + 파일명   │
│  │      ...      │  │  │      ...      │                        │
│  └──────────────┘  │  └──────────────┘                        │
├───────────────────┴─────────────────────────────────────────┤
│  기능 버튼바: F3보기 F4편집 F5복사 F6이동 F7새폴더 F8삭제        │
├────────────────────────────────────────────────────────────┤
│  상태바: 선택 개수·용량 | 진행 중 작업 pill(복사 62%) | 드라이브  │
└────────────────────────────────────────────────────────────┘
```

- 좌측에 접이식 사이드바(즐겨찾기/드라이브 트리)를 P1에서 추가 — MVP는 패널 상단 드롭다운으로 대체해 레이아웃 단순화.
- 활성 패널은 `Primary` 색상의 1px 테두리(포커스 링)로 구분 (Tab 키로 패널 전환).

## 7. 핵심 컴포넌트

- **PaneCard**: Saturn `GlassCard`를 얇게 변형 — 배경은 `surface`, 알파 블렌드 틴트는 활성 패널일 때만 `Primary` 4~6%로 미세 적용, 비활성 패널은 플랫.
- **FileRow**: 아이콘 칩 + 파일명(1줄, 말줄임) + 크기 + 수정일. 선택 시 `Primary` 10~14% 알파 배경, 호버 시 `surfaceHi`. **키보드 커서**(↑/↓로 이동한 위치)는 이 배경색과 별개로 얇은 `Primary` 테두리로 표시한다 — 사용자 피드백으로 "마우스 클릭=바로 다중선택 진입, 화살표+스페이스=커서 이동과 다중선택 진입을 분리"하는 모델을 도입하면서, 커서와 "진짜 선택"이 서로 다른 상태가 될 수 있어졌기 때문. 마우스 클릭은 여전히 클릭 즉시 그 항목 하나로 선택을 바꾼다(기존 동작 그대로).
- **OperationToast/Panel**: 하단 상태바 위에 슬라이드업되는 진행률 카드 — 파일별 진행률 바, 취소(X)/일시정지(∥) 버튼, 여러 작업 동시 진행 시 리스트로 확장.
- **ConflictDialog**: 덮어쓰기/건너뛰기/이름변경/모두 적용 4버튼, 좌우 파일 정보(크기·수정일) 비교 표시.
- **ContextMenu** (구현 완료): 우클릭(`InkWell.onSecondaryTapDown`)한 위치에 Flutter 자체 `showMenu`를 띄운다 — OS 네이티브 메뉴가 아니라 드라이브 선택기 등과 같은 방식이라 macOS/Windows/Linux 분기 없이 동일하게 동작한다. 우클릭한 항목이 이미 다중선택(2개 이상)에 속해 있으면 그 다중선택 전체를, 아니면 그 항목 하나로 선택을 바꾼 뒤 대상으로 삼는다. 항목 구성은 대상 개수·로컬여부에 따라 동적으로 달라짐(열기/보기/이름변경/복사/이동/삭제/압축/압축풀기/속성/즐겨찾기 추가).
- **ConnectionDialog(SMB)** (구현 후 수정): 호스트/공유명만 입력받는다 — 초안에서 계획했던 비밀번호 필드/`flutter_secure_storage` 저장은 실제로는 필요 없었다. OS 마운트 방식(A안 확정)이라 비밀번호는 OS의 서버 연결 다이얼로그가 직접 받기 때문. "이 서버 정보 저장" 체크박스로 host/공유명만(비밀번호 제외) 재사용 목록에 남긴다.
- **ConnectionDialog(FTP)**: SMB와 달리 앱이 프로토콜을 직접 구현하므로 호스트/포트/사용자명/비밀번호를 실제로 입력받는다 — 비밀번호는 연결에만 쓰고 저장하지 않으며, "익명 로그인" 체크박스로 로그인 필드를 감춘다. 저장된 서버 목록(비밀번호 제외)을 클릭하면 필드가 채워진다.
- **드래그 피드백(`_DragFeedback`)**: 드래그 중 커서를 따라다니는 작은 카드(아이콘+파일명, 다중이면 "N개 항목"). 드롭 가능한 폴더 행 위에 올라가면 해당 행 배경이 더 진한 `Primary` 틴트로 강조된다.

**성능 주의사항 (구현 중 발견)**: 앱 전역 `splashFactory`로 Saturn에서 물려받은 `InkSparkle`(Material 3 셰이더 기반 스플래시)을 그대로 썼더니, 파일 리스트처럼 빠르게 반복 클릭하는 조밀한 목록에서 선택 하이라이트가 수백ms 지연되는 것처럼 느껴졌다. 원인은 셰이더 애니메이션 자체의 재생 시간이지 상태 갱신 로직이 아니다. **행(`_FileRow`)과 컬럼 헤더처럼 빠르게 반복 클릭되는 요소는 `splashFactory: NoSplash.splashFactory`로 개별 오버라이드**해 배경색 전환이 즉시 보이게 했다. 버튼류(F-바 등)는 클릭 빈도가 낮아 전역 `InkSparkle`을 유지한다.

## 8. 인터랙션 & 단축키 (Total Commander 표준 계승)

| 키 | 동작 |
|---|---|
| Tab | 활성 패널 전환 |
| F3 | 보기(Viewer) |
| F5 | 복사 (반대 패널로) |
| F6 | 이동 |
| F7 | 새 폴더 |
| F8 / Delete | 삭제 |
| F2 | 이름 변경 |
| Ctrl+클릭 / Shift+클릭 | 다중 선택 |
| Ctrl+A | 전체 선택 |
| ↑ / ↓ | 커서 이동 (마우스 클릭과 달리 다중선택 자체는 안 바꾼다) |
| Space | 커서가 가리키는 항목을 다중선택에 넣거나 뺀다 (토글) |
| `/` 또는 타이핑 시작 | 퀵서치(필터) |
| Ctrl+Tab | 반대 패널 경로를 현재 패널에 미러링 |
| Ctrl/Cmd+` | 현재 경로에서 터미널 열기 (사용자 요청으로 추가) |

**드래그앤드롭** (구현 완료, 최초 초안에서 규칙 수정됨): 패널 내부 위치와 무관하게 **같은 드라이브 사이는 이동, 다른 드라이브 사이는 복사**가 기본 동작이다 (Windows 탐색기/macOS Finder 관례). 드롭 대상이 폴더 행이면 그 폴더 안으로, 빈 공간이면 패널의 현재 경로로 들어간다. 자기 자신/자신의 하위 폴더로의 드롭은 무시하거나 경고를 띄운다. 수정자 키(Alt/Cmd)로 강제 복사/이동 전환은 아직 없음 — 필요성이 확인되면 추가 예정.

## 9. 반응형/윈도우 크기

- 최소 윈도우 크기: 960×600 (2-pane 최소 가독폭 기준 패널당 ~440px)
- 960px 미만은 지원하지 않음(데스크톱 전용이므로 모바일 대응 레이아웃 불필요)
- 패널 폭 비율은 드래그 가능한 스플리터로 조정, 세션 간 유지(설정 저장)

## 10. 접근성

- 텍스트 대비: 다크/라이트 모두 WCAG AA 이상 (Saturn 팔레트 값 기준 확인됨)
- 포커스 링: 모든 상호작용 요소에 `Primary` 2px 아웃라인 (키보드 내비게이션 시각화)
- 아이콘 전용 버튼에는 툴팁 + 스크린리더 라벨 병행

## 다음 단계
1. ~~기능 리스트 확정 및 MVP 우선순위 태깅~~ — 완료
2. ~~아키텍처 설계~~ — 완료 (SMB는 OS 마운트 방식으로 확정)
3. ~~UI/UX 설계~~ — 완료 (본 문서, 이후 구현 중 발견한 수정사항 반영됨)
4. ~~프로젝트 초기 세팅~~ — 완료 (`flutter_riverpod`/`flutter_svg`/`window_manager` 등 패키지 추가, Saturn 테마 파일 이식, macOS 빌드로 실제 다크/라이트 렌더링 확인)
5. P0 기능 구현 (다음 작업)
